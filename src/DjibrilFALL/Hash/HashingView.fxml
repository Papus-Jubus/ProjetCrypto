<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.VBox?>

<VBox prefHeight="500.0" prefWidth="450.0" style="-fx-background-color: #f0f4f8;" xmlns="http://javafx.com/javafx/23" xmlns:fx="http://javafx.com/fxml/1" fx:controller="projetcrypto.Hash.HashingViewController">
    <padding>
        <Insets bottom="15" left="15" right="15" top="15" />
    </padding>
    <spacing>15</spacing>

    <Label style="-fx-font-size: 24; -fx-font-weight: bold; -fx-text-fill: #333;" text="Hachage et MAC" />

    <!-- Choix de l'algorithme de hachage -->
    <HBox spacing="10">
        <ComboBox fx:id="hashAlgorithmComboBox" onAction="#onHashAlgorithmSelected" prefWidth="180.0" promptText="Choisissez l'algorithme de hachage" />
        <ComboBox fx:id="keyAlgorithmComboBox" onAction="#onHashAlgorithmSelected" prefWidth="180.0" promptText="Choisissez l'algorithme de clé" />
    </HBox>

    <!-- Chargement du fichier à hacher -->
    <HBox spacing="10">
        <Button fx:id="loadDataFileButton" onAction="#loadDataFile" style="-fx-background-color: #4CAF50; -fx-text-fill: white;" text="Charger le fichier à hacher" />
    </HBox>
    <TextField fx:id="dataFilePathTextField" editable="false" promptText="Chemin du fichier à hacher" />

    <!-- Chargement du fichier de résultats -->
    <HBox spacing="10">
        <Button fx:id="loadResultFileButton" onAction="#loadResultFile" style="-fx-background-color: #4CAF50; -fx-text-fill: white;" text="Choisir le fichier de résultat" />
    </HBox>
    <TextField fx:id="resultFilePathTextField" editable="false" promptText="Chemin du fichier de résultats" />

    <!-- Champ pour entrer la clé -->
    <HBox spacing="10">
        <TextField fx:id="keyInputTextField" promptText="Entrez votre clé" />
    </HBox>

    <!-- Boutons pour hacher et sauvegarder -->
    <HBox spacing="10">
        <Button fx:id="hashButton" onAction="#hashData" style="-fx-background-color: #2196F3; -fx-text-fill: white;" text="Hacher" />
        <Button fx:id="saveResultButton" onAction="#saveResultToFile" style="-fx-background-color: #FF9800; -fx-text-fill: white;" text="Sauvegarder le Résultat" />
    </HBox>

    <!-- Affichage des messages de résultat -->
    <Label fx:id="resultLabel" style="-fx-font-size: 18; -fx-text-fill: #333;" text="Résultat" />

    <Region />
</VBox>
